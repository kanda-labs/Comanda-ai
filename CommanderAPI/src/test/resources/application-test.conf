ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ classes, resources ]
    }
    
    application {
        modules = [ kandalabs.commander.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:sqlite:file:test?mode=memory&cache=shared"
    driver = "org.sqlite.JDBC"
}

logging {
    level = DEBUG
}

